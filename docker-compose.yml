services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aaa
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d aaa"]
      interval: 3s
      timeout: 3s
      retries: 20

  api:
    build: .
    environment:
      ADDR: ":8080"
      DB_DRIVER: "postgres"
      DB_DSN: "postgres://postgres:postgres@db:5432/aaa?sslmode=disable"
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
